suite: membership-sync deployment
templates:
  - templates/deployment-membership-sync.yaml
  - templates/secret.yaml
release:
  name: test-release
  namespace: test-ns
tests:
  - it: should not render deployment by default (disabled)
    template: templates/deployment-membership-sync.yaml
    asserts:
      - hasDocuments:
          count: 0

  - it: should render when enabled
    template: templates/deployment-membership-sync.yaml
    set:
      agents.membershipSync.enabled: true
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: metadata.name
          value: test-release-waldur-site-agent-membership-sync

  - it: should set correct component label
    template: templates/deployment-membership-sync.yaml
    set:
      agents.membershipSync.enabled: true
    asserts:
      - equal:
          path: metadata.labels["app.kubernetes.io/component"]
          value: membership-sync
      - equal:
          path: spec.template.metadata.labels["app.kubernetes.io/component"]
          value: membership-sync

  - it: should pass correct mode argument
    template: templates/deployment-membership-sync.yaml
    set:
      agents.membershipSync.enabled: true
    asserts:
      - contains:
          path: spec.template.spec.containers[0].args
          content: "--mode"
      - contains:
          path: spec.template.spec.containers[0].args
          content: "membership_sync"
      - contains:
          path: spec.template.spec.containers[0].args
          content: "--config-file"

  - it: should set container name
    template: templates/deployment-membership-sync.yaml
    set:
      agents.membershipSync.enabled: true
    asserts:
      - equal:
          path: spec.template.spec.containers[0].name
          value: membership-sync

  - it: should set replicas from values
    template: templates/deployment-membership-sync.yaml
    set:
      agents.membershipSync.enabled: true
      agents.membershipSync.replicas: 2
    asserts:
      - equal:
          path: spec.replicas
          value: 2

  - it: should use correct selector labels
    template: templates/deployment-membership-sync.yaml
    set:
      agents.membershipSync.enabled: true
    asserts:
      - equal:
          path: spec.selector.matchLabels["app.kubernetes.io/component"]
          value: membership-sync

  - it: should include health check probes with correct command
    template: templates/deployment-membership-sync.yaml
    set:
      agents.membershipSync.enabled: true
      healthCheck.enabled: true
    asserts:
      - equal:
          path: spec.template.spec.containers[0].livenessProbe.exec.command
          value:
            - waldur_site_healthz
            - --liveness-only
